import { ResetPassword } from "../../components";
import { StyledReset } from "./style";

export const ResetPasswordPages = () => {
  return (
    <StyledReset>
      <ResetPassword />
    </StyledReset>
  );
};
