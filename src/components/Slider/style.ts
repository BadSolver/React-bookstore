import styled from "styled-components";

export const StyledSlider = styled.div`
  padding-top: 60px;
  margin: 0 auto;
  .slick-list {
    margin: 0 auto;
    width: 1120px;
  }
  .slick-track {
    gap: 60px;
    &:first-child {
      margin-left: 40px;
    }
  }
`;
